#include "wire.hpp"

namespace DCSim {
	/*Level Wire::get() const {
		return state->level;
	}

	void Wire::set(Level new_level) {
		
	}*/
	/*Level Wire::get() const {
		Level tmp=level;
		each([&tmp](const Wire* cur){
			tmp+=cur->level;
		});
		return tmp;
	}

	void Wire::set(Level new_level) {
		if(level!=new_level){
			level=new_level;
		}
	}*/
} // DCSim